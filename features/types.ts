// Objects and interfaces 
const today = new Date();
today.getMonth();

const person = {
    age : 20,
    name : "Jesus",

}

// We can define a variable and TS will know that the
// variables with this definition has every funciton and property
class Color {}

const red = new Color();


